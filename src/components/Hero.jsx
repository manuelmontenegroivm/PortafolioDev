import HeroContent from "./HeroContent";

const Hero = () => {
  return (
    <div className="relative flex flex-col min-h-screen h-full w-full max-w-[1660px] mx-auto">
      <HeroContent />
    </div>
  );
};

export default Hero;